{
  "name": "image-grabber",
  "version": "1.0.0",
  "description": "node service which requests images every x seconds and saves it to a local folder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thomas Hochörtler <hochitom@me.com>",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.1.0",
    "mkpath": "^0.1.0"
  },
  "repository": {
   "type": "git",
   "url": "git://github.com/hochitom/image-grabber.git"
 }
}
